import React from "react";

import { Button } from "reactstrap";

const Home = (props) => {
  return (
    <div>
      <Button outline color="danger">
        Primary
      </Button>
    </div>
  );
};

export default Home;
